README:
Contents:
app.py:
	The encoding python function.
	"python app.py <input-file> <min/ext>" to run.
wrapper.py:
	
non-sat-solver.py
	Michael Heavenor's non-sat sudoku implementation.
input0-5.txt:
	input files that were used to generate our example .cnf files with app.py.

input0-5.cnf:
	.cnf files from same.

picosat-954/ubcsat-1-1-0/sato3.2.1/walksat35:
	the sat solvers we tested with. The ruby implementation ofNorvig's is missing because we installed it as a library instead of a program.